# 🍪 Cookie Sniffer Terminal v1.0

A terminal-based Python tool to sniff and analyze cookies from any website using the `requests` and `rich` libraries.

## Features

- Fetches session and header cookies
- Beautiful terminal UI with `rich`
- Save cookies to a JSON file
- Works on Linux, Mac, and Windows

## Usage

```bash
python cookie_sniffer.py
